// 種: no-raw-edgeinsets と no-raw-zero-spacing。**わざと違反させています。**
// paths: ["lib/ui/"] が効くので、この階層に置く必要があります。
import 'package:flutter/material.dart';

const seedRawEdgeInsets = EdgeInsets.all(16);
const seedZeroSpacing = EdgeInsets.zero;
