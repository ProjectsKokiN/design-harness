# 種（seed）のひな形 — Web

**この中のコードは、わざと違反させてあります。** 案件の `design/seeds/` にコピーし、
`rules.json` の `exclude_paths` に `design/seeds/` を必ず入れてください。

案件固有のルール（ダークテーマの禁止・廃止クラスなど）には、案件側で種を足してください。

    python3 design/harness/tools/seed_check.py
