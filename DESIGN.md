# デザイン憲法（全案件共通）

**このファイルは全プロジェクト共通の設計基準です。** 案件側の `DESIGN.md` は
これを参照し、案件固有の変数だけを書きます（案件側にこの内容を写さないでください。
写すと、ここを直しても案件側が古いまま残ります）。

**最新への追随:** submodule を更新すると、全案件がこの最新版を読みます。

```bash
git -C design/harness pull origin main
```

2026-08-29 新設。それまで各案件の DESIGN.md（flash-compose で156行・10.5KB）に
同じ内容が手で書き写されており、共通部分を直しても他案件へ届かない状態でした。
検査エンジンを一本化したのと同じ理由で、憲法も上層に一本化します。

---

## 案件側の DESIGN.md に必ず書くこと

下の一覧は `tools/design_md_check.py` が読む**機械検査の分母**です。
見出しをこのとおりに書いてください（文言を変えると検査が落ちます）。

```required-sections
## 使うデザインシステム
## スタック
## 検証フェーズ
## 承認済みの例外
## この案件だけの決まり
```

案件側には、上の5つと**共通の憲法への参照**だけを書きます。20〜40行に収まります。
原則・品質フロア・検証の段・語彙・モデルの使い分けは**このファイルが正本**なので、
案件側に写さないでください（写しがあると `design_md_check.py` が落ちます）。

`## スタック` には `flutter` / `web` のどちらかを書きます。
下の品質フロアは、この宣言で適用範囲が決まります。

---

## 設計原則（6つ）

1. **トークン外の値を使わない。** 色・余白・角丸・タイポはすべてセマンティック
   トークン経由。生の数値・色コードの直書きは `design/rules.json` により機械的に
   検出されます。
2. **新しいコンポーネントを勝手に作らない。** `design/component-map.json` にある
   ものを再利用します。無い場合は候補を提示して人間の承認を待ちます。
3. **Figma がある画面は Figma が視覚の正。** 無い画面はトークンと既存コンポーネントの
   組み合わせで構成し、勝手な視覚的発明をしません。
4. **必要なトークンが存在しない場合は実装せず報告する。** 近い値での代用や
   新値の創作をしません。
5. **Figma にあるものは全部実装する。** コンポーネント・トークン・スタイルのうち
   「この画面では使わない」という理由で実装を省きません。**どれを作る・作らないを
   AI が判断しません。**（原則1の裏返し。1が「無いものを作らない」、これが
   「あるものは全部作る」）使わない部品があるなら、消すのは Figma 側で
   デザイナーが行います。詳細は
   `~/.claude/skills/mobile-harness-setup/references/production-gate.md` の条件7。
6. **見た目の合否は判断しない。** Figma と実装をスクリーンショットで見比べて
   合否を決めません。AI の役目は Figma のトークン・スタイル・コンポーネントで
   数値的・構造的に組み、その数値が合っているかを定量的に検証することです。
   **見た目の定性的な評価はデザイナー本人の役目。** 画面を画像に書き出す仕組みが
   ある場合、それは過去の実装を残すためであって AI の検証手段ではありません
   （動かなくても「検証できていない」と扱いません）。

## 手で写さない、生成器から作る

**Figma の値を人が読んで別のファイルに書き写す層を作りません。**
実装値の正は Figma の機械書き出し（`figma/*.json`）だけです。

aub-familywalk の実測: 検査が緑のまま Figma と食い違っていた誤り8件は、
**全部が手書きの層**から出ました（機械生成の層からは0件）。記録を書いたのも AI なので、
自分の答案を自分で採点している状態でした。手書きの値が「自動生成。手で編集しない。」
というヘッダを付けて下流に流れると、機械生成の値と見分けが付きません（誤りの洗浄）。

- 生成器の入力は書き出しだけ（`tools/gen_input_check.py` が見ます）
- 生成物が書き出しと一致することを検査する（案件の `design/gen/verify.py`）
- 照合テストは `figma/frames.json` を読む（手書きの記録を読みません）

## 品質フロア（すべての画面が満たす最低条件）

### 共通（スタックを問わない）

- ローディング・エラー・空状態の3状態を必ず設計する
- 読み上げ（スクリーンリーダー）が意味を持つ。押せる要素には識別子を付ける
- 文字と面のコントラストが WCAG AA（通常 4.5:1 / 大きい文字 3.0:1）を満たす。
  トークンの**組み合わせ自体**は `check_contrast.py` が検査済みなので、ここで見るのは
  「実際に使った組み合わせが、その許可された範囲に入っているか」
- 「視差効果を減らす」設定では、拡大・回転・波打ちを止める（色の変化だけにする）
- キーボード／スイッチ操作でたどれる。フォーカスが見える
- 案件がダークテーマを持つ場合、ライト／ダーク両方で破綻しない
  （持つかどうかは案件側の `## この案件だけの決まり` で宣言します）

### スタック: flutter（iOS / Android アプリ）

- SafeArea / ノッチ対応（画面端に UI が食い込まない）
- テキストスケール **1.3 倍**でもレイアウトが崩れない（端末の文字サイズ設定）
- タップ領域は最小 **48dp**（iOS の 44pt を満たす値として 48 に統一）
- 画面回転・分割ビューで破綻しない（対応しない場合は明示的に固定する）

### スタック: web

- ブラウザのズーム **200%** でレイアウトが崩れない（WCAG 1.4.4）
- 幅 320px（`text-spacing` 相当の狭幅）で横スクロールが出ない
- ポインタ操作のクリック領域は最小 **24px**（WCAG 2.5.8 AA）。
  タッチ主体の画面では **44px** にする
- `prefers-reduced-motion` を尊重する
- ホバーでしか出ない情報を作らない（タッチ端末で到達できなくなる）

---

## 検証の4段

**各段は「前の段が見られないもの」だけを担当します。**
**この4段の呼び名を言い換えないでください**（「数値照合」が「スクリーンショット」に
化けた実例があります）。

| 層 | 段 | 担当 | 前の段が見られないもの |
|---|---|---|---|
| 定量検査 | 1. 保存時パターン検査 | 生の色・数値・付与漏れ | — |
| 定量検査 | 2. 数値照合 | 色・寸法・余白・角丸の値 | 文字列検査は値を見ない |
| 定性検査 | 3. エージェントレビュー | 3状態・視覚階層・一貫性 | 数値照合は「意味」を見ない |
| 定性検査 | 4. 人間レビュー | 好みとブランドの判断 | 機械は好みを判断できない |

2段目・3段目が無いと、機械で判定できるズレまで人間の目視に来ます。
逆に、機械で判定できることを画像で判定しようとすると、根拠のない
「だいたい合っています」が生まれます。

**保存時 hook は関門ではありません。** hook はマシンごとの設定（git 管理外）なので、
新しいマシン・新しいセッションでは黙って無効になります。**関門は CI と pre-push** です。
hook は書いている最中の即時フィードバックとして置きます。

## 止まったときの区分

実装を止めたときは、次のどれかで報告します（語彙の正本は `vocab/_vocab.json`）。

| 区分 | 意味 | 次にすること |
|---|---|---|
| `figma-missing` | Figma に定義が無い | デザイナーが Figma に足す |
| `token-missing` | 必要なトークンが無い | デザイナーがトークンを足す |
| `export-stale` | 書き出しが Figma より古い | 書き出し直す |
| `needs-decision` | 仕様の判断が要る | ユーザーが決める |

**近い値で代用して進めません。** 止まったことを報告するのが正しい振る舞いです。

## 完了レポートの書式

**残っている最大の限界を先に書きます。** 「できたこと」を並べてから最後に
限界を書くと、読む側は合格したと受け取ります。

1. **残っている限界**（数値で検証できていない項目・目視が必要な項目）
2. 実施した検査と結果（件数）
3. 止まった項目（上の区分で）
4. ルールへの還元の提案（他画面でも起きるなら `rules.json` への追記案）

## モデルの使い分け

| 作業 | モデル / エフォート |
|---|---|
| 画面実装・部品実装・検査の実行 | Opus / medium |
| デザインシステムの設計・カタログ構成・視覚品質の判断 | Fable / high |
| 層をまたぐ設計・大規模リファクタ・原因不明のバグ | Fable / xhigh〜max |

---

## 関係する正本

| 内容 | 場所 |
|---|---|
| 本番リリースの合格条件 | `~/.claude/skills/mobile-harness-setup/references/production-gate.md` |
| ハーネスの構築手順 | `~/.claude/skills/mobile-harness-setup/` |
| 画面実装の手順 | `~/.claude/skills/mobile-implement-ui/` |
| 検査エンジン・道具 | このリポジトリの `engine/` `tools/` |
| Web の参照実装 | `qnd-database/site/design/harness/` |
