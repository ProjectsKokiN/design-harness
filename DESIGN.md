# デザイン憲法（全案件共通）

**このファイルは全プロジェクト共通の設計基準です。** 案件側の `DESIGN.md` は
これを参照し、案件固有の変数だけを書きます（案件側にこの内容を写さないでください。
写すと、ここを直しても案件側が古いまま残ります）。

**最新への追随:** submodule を更新すると、全案件がこの最新版を読みます。

```bash
git -C design/harness pull origin main
```

2026-08-29 新設。それまで各案件の DESIGN.md（FlashEnglish で156行・10.5KB）に
同じ内容が手で書き写されており、共通部分を直しても他案件へ届かない状態でした。
検査エンジンを一本化したのと同じ理由で、憲法も上層に一本化します。

---

## 案件側の DESIGN.md に必ず書くこと

下の一覧は `tools/design_md_check.py` が読む**機械検査の分母**です。
見出しをこのとおりに書いてください（文言を変えると検査が落ちます）。

```required-sections
## 使うデザインシステム
## スタック
## 検証フェーズ
## 承認済みの例外
## この案件だけの決まり
```

案件側には、上の5つと**共通の憲法への参照**だけを書きます。20〜40行に収まります。
原則・品質フロア・検証の段・語彙・モデルの使い分けは**このファイルが正本**なので、
案件側に写さないでください（写しがあると `design_md_check.py` が落ちます）。

`## スタック` には `flutter` / `web` のどちらかを書きます。
下の品質フロアは、この宣言で適用範囲が決まります。

---

## 設計原則（6つ）

1. **トークン外の値を使わない。** 色・余白・角丸・タイポはすべてセマンティック
   トークン経由。生の数値・色コードの直書きは `design/rules.json` により機械的に
   検出されます。
2. **新しいコンポーネントを勝手に作らない。** `design/component-map.json` にある
   ものを再利用します。無い場合は候補を提示して人間の承認を待ちます。
3. **Figma がある画面は Figma が視覚の正。** 無い画面はトークンと既存コンポーネントの
   組み合わせで構成し、勝手な視覚的発明をしません。
4. **必要なトークンが存在しない場合は実装せず報告する。** 近い値での代用や
   新値の創作をしません。
5. **Figma にあるものは全部実装する。** コンポーネント・トークン・スタイルのうち
   「この画面では使わない」という理由で実装を省きません。**どれを作る・作らないを
   AI が判断しません。**（原則1の裏返し。1が「無いものを作らない」、これが
   「あるものは全部作る」）使わない部品があるなら、消すのは Figma 側で
   デザイナーが行います。詳細は
   `~/.claude/skills/mobile-harness-setup/references/production-gate.md` の条件7。
6. **見た目の合否は判断しない。** Figma と実装をスクリーンショットで見比べて
   合否を決めません。AI の役目は Figma のトークン・スタイル・コンポーネントで
   数値的・構造的に組み、その数値が合っているかを定量的に検証することです。
   **見た目の定性的な評価はデザイナー本人の役目。** 画面を画像に書き出す仕組みが
   ある場合、それは過去の実装を残すためであって AI の検証手段ではありません
   （動かなくても「検証できていない」と扱いません）。

## 画面は状態を指定して開けるようにする

**「画像で合否を判断しない。見た目の判断はデザイナー本人に残す」と決めている
のに、その判断に必要な状態を出す仕組みがありません。**

見本帳（カタログ）は**部品単位**で、画面を状態指定で開けません。エラー状態・
空状態・読み込み中はアプリを操作しても再現しにくく、結果として
**「人が見る」と決めた層が、実際には見られないまま通ります。**

    /catalog?screen=ALBUM&state=empty
    /catalog?screen=ALBUM&state=error

画面ごとに**出せる状態を宣言**し、見本帳から開けるようにします。宣言は
`design/screen-states.json`（`hollow_check` の形2 の材料でもあります）。

**どの状態を出すかは機械では決まりません。** 「この画面にエラー状態がある」
ことを知っているのは実装だけなので、**実装が宣言し、検査は宣言と実物の一致を
見る**形になります。

## 画面上下の安全領域は、差分だけ足す

**Figma の値を土台にして、足りないぶんだけ足します。**

    上に足す = max(0, 端末の上の余白 − Header の paddingTop)
    下に足す = max(0, 端末の下の余白 − Footer の paddingBottom)

`padding` ではなく **`viewPadding`** を見ます（キーボードが出ている間も値が
変わらないため）。**見込みの値（48 / 24）はコードに書きません。**
Figma の書き出し（部品の `padding`）から読みます。

| 機体 | 上 | 下 |
|---|---|---|
| Android 360x640 / iPhone SE | **±0**（Figma のまま） | ±0 |
| iPhone 17 Pro Max（59 / 34） | **+11** | **+10** |

### 3つの決めごと

1. **画面ぜんぶを `SafeArea` で包まない。** 包むと Figma の 48 に端末の 59 が
   重なって 107 になり、背景も切れます。**aub は過去に一度それで壊しています**
2. **安全領域に帯の塗りを入れない**（案件によっては塗る。**どちらかを
   `DECISIONS.md` に残す**——aub では1日で決定が2回ひっくり返りました）
3. **帯は縮めず、伸ばす**（中身を縮めると小さい機体で先に苦しくなる）

守る機体と `viewPadding` は `design/devices.json` に宣言します
（ひな形: `templates/devices.json.template`）。**検査は最小と最大の両方で
回します。** 安全領域が 0 の機体で**1ピクセルも変わらないこと**が差分方式の要で、
そこを見ないと「全部足す」実装が通ります。

## 決定は取り消せる形で書く

**追記式の記録は、前の決定が生きているように読めます。**

    ## 2026-09-03（2回目）安全領域は白で塗る

    **supersedes: 2026-09-03（1回目）安全領域には何も塗らない**

**取り消しは消しません**（経緯が要る）。ただし**取り消し済みと機械で分かる**
ようにします。`tools/decisions_check.py` が、取り消された決定を実装のコメントが
引用していたら落とします（**語尾が違っても捕まえます**）。

aub の実害（2026-09-03）: 1日で2回ひっくり返り、実装コメントに
「安全領域には何も塗りません」が残ったまま白で塗る実装になりました。
**次に読む人（AI）は矛盾した2つの正を見ます。**

### 指示の値から節点を引く

**「Figma では N」と言われたら、まず N を持つ節点を書き出しから列挙します。**
**候補が2つ以上あれば、直す前に「どちらですか」と聞きます。**

aub の実害: 「figma では 20 ピクセル」の指示に対し、20 を持つ節点（紙）と
持たない節点（外枠）の両方があるのに、**聞かずに外枠を直して戻しました。**

### 「1つ測って合っていた」で報告を終えない

**同じ枠の中にある要素を全部測って並べます。**

aub の実害: 「上側の余白が空きすぎ」の指摘に、**写真だけ**を測って
「20 です」と報告しました。**板を見ていませんでした**（そこだけ 45）。

## 同じ状態を2か所に持たない

**行き先と表示が別々の持ち主にあると、食い違ったときに行き止まりになります。**

aub の実害（2026-09-03）: 行き先は go_router、送りの位置は `PageController` が
持っていました。帯（題字とボトムナビ）は**送りの位置だけ**を見ていたので、
払って戻したあとにタブを押すと、**行き先は `/bingo`、帯は PROGRESS** に
なりました。

そうなると

1. 見えている画面のボタンは「別のタブ」なので押せるが、行き先はもう `/bingo` なので**何も起きない**
2. 戻りたいタブのボタンは「いまのタブ」扱いなので**押せない**

**どこも押せません。行き止まりです。**

**「開いている画面のボタンは押せない」という正しい作りが、状態の食い違いと
組み合わさって逃げ道を消しました。** 正しさが2つ重なって行き止まりを作る形は、
どちらか片方を見ても分かりません。**状態の持ち主を1つにするのが唯一の防ぎ方**です。

## 壊して落ちるのを見た検査だけを数える

**「落ちることを見てから採用する」を、記憶ではなく記録にする。**

aub-familywalk の2日で、**検査は回っているのに何も見ていない**という穴が5件出た。
全部「緑」で、**うち3件は仕込み試験（わざと壊して落ちるか見る）で見つかった。**
回すかどうかが人の記憶に依っているのが、いちばん薄いところだった。

だから記録として残す（`design/sabotage.json`）。

```json
{
  "test/design/device_widths_test.dart": [
    { "壊し方": "BINGO の盤を 350 固定に戻す", "落ちた": true, "at": "2026-09-03" },
    { "壊し方": "型紙を 82.4 の固定に戻す",    "落ちた": true, "at": "2026-09-03" }
  ]
}
```

`hollow_check.py` が次を落とす。

- **壊しても落ちなかった**仕込みがある（`"落ちた": false`）
- 記録が**その検査の最後の変更より古い**（直したあとに壊して試していない）
- 記録にある検査が**もう無い**（記録のほうが古くなっている）

最後の日付は `git log` から取るので、**書いた日付を自己申告するだけでは通らない。**

## 手で写さない、生成器から作る

**Figma の値を人が読んで別のファイルに書き写す層を作りません。**
実装値の正は Figma の機械書き出し（`figma/*.json`）だけです。

aub-familywalk の実測: 検査が緑のまま Figma と食い違っていた誤り8件は、
**全部が手書きの層**から出ました（機械生成の層からは0件）。記録を書いたのも AI なので、
自分の答案を自分で採点している状態でした。手書きの値が「自動生成。手で編集しない。」
というヘッダを付けて下流に流れると、機械生成の値と見分けが付きません（誤りの洗浄）。

- 生成器の入力は書き出しだけ（`tools/gen_input_check.py` が見ます）
- 生成物が書き出しと一致することを検査する（案件の `design/gen/verify.py`）
- 照合テストは `figma/frames.json` を読む（手書きの記録を読みません）

### 対象の一覧も、手で書かない

**「何を見るか」の一覧を手で宣言すると、それが古くなります。**
生成物の鮮度を指紋で見るとき、**入口から辿って導出します。**

aub-familywalk の実測（2026-09-03）: カタログの鮮度の指紋が `('lib', '*.dart')`
と書かれており、**`lib` の全75件**を掛けていました。カタログが実際に読むのは35件で、
`lib/data` `lib/providers` `lib/ui/screens` は**1件も読まれません**。

そのため **Windows がデータ・ロジックを直すだけでカタログが「古い」になり**、
作り直す手（`--build`）は担当外のファイルを書くので別の段が落ちました。
**どちらを選んでも NG が1つ残る**状態が2日続いています。

**狭めた一覧を手で書いても同じことが起きます**（部品が増えて一覧が古くなり、
古くなったことに誰も気づかない）。だから辿ります。

> **カタログが新しいファイルを読み始めれば、その日から指紋に入る。**

- 指紋の対象は**入口から import を辿って**出す（宣言しない）
- **改行を LF に揃えてから**掛ける（CRLF の作業ツリーで指紋が変わらないように）
- 記録先そのもの（`build_info.g.dart` など）は指紋に入れない
- 実例: aub-familywalk の `design/catalog_build.py` の `catalog_sources()`

## 品質フロア（すべての画面が満たす最低条件）

### 共通（スタックを問わない）

- ローディング・エラー・空状態の3状態を必ず設計する
- 読み上げ（スクリーンリーダー）が意味を持つ。押せる要素には識別子を付ける
- 文字と面のコントラストが WCAG AA（通常 4.5:1 / 大きい文字 3.0:1）を満たす。
  トークンの**組み合わせ自体**は `check_contrast.py` が検査済みなので、ここで見るのは
  「実際に使った組み合わせが、その許可された範囲に入っているか」
- 「視差効果を減らす」設定では、拡大・回転・波打ちを止める（色の変化だけにする）
- キーボード／スイッチ操作でたどれる。フォーカスが見える
- 案件がダークテーマを持つ場合、ライト／ダーク両方で破綻しない
  （持つかどうかは案件側の `## この案件だけの決まり` で宣言します）

### スタック: flutter（iOS / Android アプリ）

- SafeArea / ノッチ対応（画面端に UI が食い込まない）
- テキストスケール **1.3 倍**でもレイアウトが崩れない（端末の文字サイズ設定）
- タップ領域は最小 **48dp**（iOS の 44pt を満たす値として 48 に統一）
- 画面回転・分割ビューで破綻しない（対応しない場合は明示的に固定する）

### スタック: web

- ブラウザのズーム **200%** でレイアウトが崩れない（WCAG 1.4.4）
- 幅 320px（`text-spacing` 相当の狭幅）で横スクロールが出ない
- ポインタ操作のクリック領域は最小 **24px**（WCAG 2.5.8 AA）。
  タッチ主体の画面では **44px** にする
- `prefers-reduced-motion` を尊重する
- ホバーでしか出ない情報を作らない（タッチ端末で到達できなくなる）

---

## 検証の4段

**各段は「前の段が見られないもの」だけを担当します。**
**この4段の呼び名を言い換えないでください**（「数値照合」が「スクリーンショット」に
化けた実例があります）。

| 層 | 段 | 担当 | 前の段が見られないもの |
|---|---|---|---|
| 定量検査 | 1. 保存時パターン検査 | 生の色・数値・付与漏れ | — |
| 定量検査 | 2. 数値照合 | 色・寸法・余白・角丸の値 | 文字列検査は値を見ない |
| 定性検査 | 3. エージェントレビュー | 3状態・視覚階層・一貫性 | 数値照合は「意味」を見ない |
| 定性検査 | 4. 人間レビュー | 好みとブランドの判断 | 機械は好みを判断できない |

2段目・3段目が無いと、機械で判定できるズレまで人間の目視に来ます。
逆に、機械で判定できることを画像で判定しようとすると、根拠のない
「だいたい合っています」が生まれます。

**保存時 hook は関門ではありません。** hook はマシンごとの設定（git 管理外）なので、
新しいマシン・新しいセッションでは黙って無効になります。**関門は CI と pre-push** です。
hook は書いている最中の即時フィードバックとして置きます。

## 止まったときの区分

実装を止めたときは、次のどれかで報告します（語彙の正本は `vocab/_vocab.json`）。

| 区分 | 意味 | 次にすること |
|---|---|---|
| `figma-missing` | Figma に定義が無い | デザイナーが Figma に足す |
| `token-missing` | 必要なトークンが無い | デザイナーがトークンを足す |
| `export-stale` | 書き出しが Figma より古い | 書き出し直す |
| `needs-decision` | 仕様の判断が要る | ユーザーが決める |

**近い値で代用して進めません。** 止まったことを報告するのが正しい振る舞いです。

## 完了レポートの書式

**残っている最大の限界を先に書きます。** 「できたこと」を並べてから最後に
限界を書くと、読む側は合格したと受け取ります。

1. **残っている限界** — `tools/gap_report.py` の出力を**そのまま貼ります**
2. 実施した検査と結果（件数）
3. 止まった項目（上の区分で）
4. ルールへの還元の提案（他画面でも起きるなら `rules.json` への追記案）

**1 を自分の言葉で書き直さないでください**（2026-08-29）。限界を書くのが AI 自身だと、
書き落とせば報告書は合格したように読めます。記録層を廃止したのと同じ構図です。

実害: `check_flutter_gaps.py`（現 `tools/check_render_gaps.py`）が「× 0件 / exit 0」と報告した裏で、Figma の26セットの
うち10セットに評価が付いていませんでした。「0件」は「違反が 0 件」であって
「10セットを見ていない」とは書いていません。**出力上はまったく同じに見えます。**

`gap_report.py` が出すもの: 1件も発火していないルール（**種があれば「コードが綺麗」・
無ければ「効いているか不明」**と区別）／ `harness-ignore` で除外した箇所と理由／
照合テストが見当たらない画面／参照していない Figma ページ／数値で検証できないと
宣言した項目。

## 検査が働いていることを、どう確かめるか

**違反が 0 件なのと、そもそも見ていないのは、出力上まったく同じに見えます。**
これを区別するための道具が3つあります。

| 道具 | 数えるもの | 捕まえる失敗 |
|---|---|---|
| `attack/engine_attack_test.py` | エンジンの挙動 | エンジンが壊れた |
| `expected_targets`（rules.json） | 読んだファイル数 | 検査対象が黙って狭まった（QnD: 除外1語で19ファイル） |
| `tools/seed_check.py` | **ルールごとの発火件数** | **ルールが黙って死んだ**（aub: 幽霊ルールが一度も走らなかった） |

`design/seeds/` には**わざと違反させたコード**を置き、`expected.json` に
「どのルールが何件出るはずか」を宣言します。**seeds は通常の走査から必ず除外**
してください（`exclude_paths`）。

## モデルの使い分け

| 作業 | モデル / エフォート |
|---|---|
| 画面実装・部品実装・検査の実行 | Opus / medium |
| デザインシステムの設計・カタログ構成・視覚品質の判断 | Fable / high |
| 層をまたぐ設計・大規模リファクタ・原因不明のバグ | Fable / xhigh〜max |

---

## 関係する正本

| 内容 | 場所 |
|---|---|
| 本番リリースの合格条件 | `~/.claude/skills/mobile-harness-setup/references/production-gate.md` |
| ハーネスの構築手順 | `~/.claude/skills/mobile-harness-setup/` |
| 画面実装の手順 | `~/.claude/skills/mobile-implement-ui/` |
| 検査エンジン・道具 | このリポジトリの `engine/` `tools/` |
| Web の参照実装 | `qnd-database/site/design/harness/` |
