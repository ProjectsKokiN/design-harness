{
  "$meta": {
    "unit": "この案件が守る画面の大きさ。**論理ピクセル**（Flutter の dp）。",
    "なぜ要るか": "どの幅で成り立てばよいのかがどこにも書かれていないと、検査を1つの幅（Figma のアートボード）でしか回せない。伸びるべきものを固定値で写しても**必ず通る**（design-harness issue #8。aub の実機で14件出た）。",
    "決めた人": "{{誰が・いつ。実機のシミュレータで最小・最大を確認したうえで指定}}",
    "原文": "{{ユーザーの原文をそのまま}}",
    "安全領域": "`viewPadding` は**端末が使う上下の帯**（ダイナミックアイランド・ホームインジケータ・ジェスチャーバー）。実装は **Figma が見込んでいるぶんを超えた差分だけ**足す（上は Header の paddingTop、下は Footer の paddingBottom が見込み）。**安全領域には何も塗らない**か塗るかは案件の決定（DECISIONS.md に残す）。",
    "使う場所": "{{検査のファイル。全画面をこの幅で描いてはみ出しを見る}}",
    "updatedAt": "{{YYYY-MM-DD}}"
  },
  "min": [
    {"name": "{{Android スモールフォン}}", "size": [360, 640],
     "viewPadding": {"top": 0, "bottom": 0}},
    {"name": "{{iPhone SE}}", "size": [375, 667],
     "viewPadding": {"top": 0, "bottom": 0}}
  ],
  "max": [
    {"name": "{{iPhone 17 Pro Max}}", "size": [440, 956],
     "viewPadding": {"top": 59, "bottom": 34}}
  ]
}
