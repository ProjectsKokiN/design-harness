# Android 側の引き直し用: `none` で未対応の45行を、規約の文に起こしたもの（2026-09-06）

作成: **Windows (Dev-Logic)** / 対象: `analysis/build-android-2026-09-06.json`（201行）

## これは何か

段2の突き合わせで、**`依存` の列だけでは仕分けきれない**ことが分かりました
（`analysis/merge-build-2026-09-06.md`）。同じ判断が片方で `none`・もう片方で `env` や
`hardware` に置かれ、原因は**実体（コマンド）に引かれて分類していた**ことでした。

Mac mini の提案（**先に規約の文で引き直し、対応が付いたものだけ分類を直す**）に同意したので、
**Android 側の未対応45行から実体を剥がし、規約の文に起こしました。**

**この文書は棚卸しの行を1つも変更していません。** 突き合わせの材料としてだけ使ってください。
行を直すかどうかは、対応が付いてから決めます（2026-09-06 の合意: 名指しは相手・直すかは持ち主・
**直す前に持ち主が実物で確かめる**）。

## 分母

| | 数 |
|---|---:|
| Android の `依存 = none` | 69 |
| 確定した21の規約で使われている | 24 |
| **未対応** | **45** |

Mac mini の見立ては46でした。差の1行は `119` だと思われます（`none` に倒したあと、
そちらの撤回とこちらの実測で `hardware` に差し戻したため、いまは `none` ではありません）。

## 規約が取り出せたもの（30行）

**実体を剥がすと、どのプラットフォームでも言える文になります。**

| id | 工程 | 実体を剥がした規約の文 |
|---|---|---|
| 3 | 前提 | **配布前の関門は1か所にまとめ、そこを通らずに配布へ進めないようにする**（関門を複数に散らさない） |
| 12 | 取得 | **未コミットの変更があるまま配布物を作らない** |
| 14 | 取得 | **既定ブランチより遅れたまま配布物を作らない。取り込みは自動でやらず、案内して止める** |
| 15 | 取得 | **検査が参照する外部の写しが汚れていたら、破棄せず中身を確認させて止める** |
| 17 | 取得 | **未 push の変更を含む外部の写しで配布物を作らない** |
| 70 | 取得 | **依頼を読むときは、依頼が名指しした資料も読む。前回から変わっていなくても読む** |
| 22 | 検品 | **静的解析を配布前の関門に入れる** |
| 23 | 検品 | **原因が特定しやすい検査を先に単独で回し、後段の大きな検査に埋もれさせない** |
| 25 | 検品 | **禁止パターンの検査を配布前の関門に入れる**（正本はシムではなく engine 側にある） |
| 29 | 検品 | **CI は配布物を作らない。配布物を出す経路は機体の手順に限る** |
| 38 | 検品 | **今回のビルドで生成された成果物であることを確かめてから配る**（古い成果物を配らない） |
| 30 | ビルド | **配布物にビルド元のコミットを刻む** |
| 31 | ビルド | **版番号は毎回一意にし、どのビルドが端末に入っているかを判別できるようにする** |
| 37 | ビルド | **各段の終了コードを直後に見て、失敗したらそこで止める** |
| 39 | 配布 | **成果物をローカルで改名しない**（通信が失敗しても同じコマンドで再実行できるように） |
| 41 | 配布 | **配布物の名前を決める場所を1か所にする** |
| 51 | 配布 | **新しい配布物が置けたことを確かめてから、古いものを片付ける** |
| 92 | 配布 | **同じ内容を二重に置かない。上書きは明示の指示があるときだけ** |
| 142 | 配布 | **配布に使う資格は必要最小の範囲に限る** |
| 56 | 報告 | **配布が完了したことを確かめるまで完了報告しない**（ビルドと配布を一つの完了単位として扱う） |
| 57 | 報告 | **失敗しても別の保存先・経路へ切り替えない。同じ経路を再実行する** |
| 58 | 報告 | **前段が失敗したら、既存の成果物を配らない** |
| 53 | 記録 | **ビルド元の情報を、作業中の人が見える形で出す** |
| 99 | 記録 | **記録には必須の節を決め、欠けていたら機械で落とす** |
| 100 | 記録 | **完了した依頼は消さずに保管所へ移し、索引から消す** |
| 104 | 報告 | **報告は「何を見て・どうだったか」の表で書く** |
| 105 | 報告 | **分からなかったこと・直せなかったことをそのまま書き戻す。数を書くときは、いつ時点の数かを添える** |
| 138 | 記録 | **その回で初めて踏んだものを宛先つきで残す。無ければ「無し」と書く** |
| 139 | 記録 | **依頼の出し入れは道具で行い、手で書き換えない** |
| 121 | 仮想機の確認 | **警告の件数を正常の根拠にしない**（0件でも崩れていないとは限らない） |

### 既に確定している規約と重なりそうなもの（3件）

こちらからは重複かどうか判定できません。**そちらで見てください。**

| id | 文 | 重なりそうな確定規約 |
|---|---|---|
| 30 | 配布物にビルド元のコミットを刻む | 「配布物と記録に元コミットの SHA を載せる」（iOS 334 × Android 54） |
| 125 | **全画面を歩かない。変わった画面だけ見る** | 「全画面を歩かない。差分と名指しの画面だけ」（iOS 221 × Android 85） |
| 15 / 17 | 外部の写しのクリーンさ・未 push | 「検査が参照する外部の写しの版を先に揃える」（iOS 18 × Android 69） |

`125` は文がほぼ同じなので、**確定21の Android 側の根拠に足せる**かもしれません。

## 規約が取り出せなかったもの（15行）

**どれも「実装がこうなっている」という事実の記録**で、規約ではありません。
**共有層に上がる余地はありません**が、片方にしか無い穴として読む価値があります。

| id | 何の記録か | 種類 |
|---|---|---|
| 24 | `flutter test` を並列度の指定なしで回す（同じ案件の `verify.sh` は `--concurrency=1` を持つのに、preflight 側は持たない） | **案件内の食い違い** |
| 27 | 配布経路の関門が push 経路の関門の部分集合になっている（`verify.sh` 固有の段が配布経路に入らない） | **穴** |
| 35 | `--dart-define=BUILD_REV` を渡しているが、受け側が実装に1件も無い | **穴（渡した値が読まれていない）** |
| 73 | 明示の `flutter pub get` 手順が依頼にも文書にも無い（ビルドに任せている） | **穴** |
| 151 | 上げた後の照合をしない（`create` の応答をそのまま信じる） | **穴（flash 側は照合する）** |
| 134 | `emulator_runs.json` を読む段が無い（書いた本人だけが読む） | **穴（記録に読み手がいない）** |
| 127 | flash-compose には仮想機の確認の段そのものが無い | **穴** |
| 32 | 31 の根拠になった経緯（2026-08-27 のユーザー指摘） | 根拠の記録 |
| 42 | 41 の根拠になった経緯（2026-09-04 の実測） | 根拠の記録 |
| 135 | 開かなかった台があっても記録は書かれる（書き込みが先・NG 判定が後） | 実装の順序 |
| 136 | `--small` / `--large` では記録を書き換えない | 実装の状態 |
| 149 | 送信の失敗を種類で見分けずに送り直す | 実装の状態 |
| 150 | 進み具合の表示（flash は塊ごと・aub は10%刻み） | 実装の粒度 |
| 155 | 引数の受け取り方 | 実装の状態 |
| 104 の残り | （`104` は規約側にも入れた。表の書き方そのもの） | — |

## 読み方の注意

- **「規約が取り出せた」は「共有層に上がる」ではありません。** iOS 側に対応する行があり、
  反証に耐えたものだけが上がります。ここは**候補の一覧**です
- **文はこちらが起こしたものです。** 元の行の `手順` は変えていないので、
  対応を付けるときは**必ず元の行を読んでください**（言い換えで条件を落とす事故を
  `200` で1度やっています）
- 「穴」に分類した7件（24・27・35・73・151・134・127）は、**共有層ではなく
  こちらの案件の課題**です。段3が片付いたあとに拾います
