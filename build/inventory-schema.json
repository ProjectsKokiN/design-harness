{
  "$手で書き換えない": "いいえ。これは手で書く定義です（生成物ではありません）",
  "$meta": {
    "名前": "ビルドの棚卸しの項目表",
    "作成": "2026-09-06",
    "目的": "Mac mini（iOS）と Windows（Android）が、同じ項目でビルドの実態を書き出す。両方が揃ってから統合の可否を判定するため",
    "なぜ項目を固定するか": "2026-09-06 の機体分析では .md と .json の対で出したので、78件を項目単位で突き合わせられた。散文だけだと読み手が解釈し直すことになり、統合の判定が人の印象になる",
    "使い方": "各機体は analysis/build-<platform>-<日付>.json をこの形で書く。python3 build/inventory_check.py <file> が通ることを確認してから push する",
    "統合の判定は自分でしない": "各行の `依存` を事実として書くだけでよい。統合するかどうかは両方が揃ってから決める（下の `統合の判定規則` は、その時に機械で当てる規則）",
    "統合の判定規則": {
      "依存 = none": "統合する（design-harness/build/ に置く）",
      "依存 = platform": "**統合しない。** そのプラットフォームのスキルに置く（iOS=Mac mini・Android=Windows）。build/ には置かない",
      "依存 = env | credential | hardware": "機体で分ける（各機体のスキルに置く）",
      "依存 = project": "案件の値として外に出す（<案件>/design/<platform>.json）"
    },
    "改訂": [
      "2026-09-06 初版",
      "2026-09-06 改訂1: 依存に `platform` を足し、案件差に `planttalk` / `全部` / `該当なし` を足した。粒度の規約を明記（iOS 側の反証の申し送りによる）"
    ]
  },
  "工程": {
    "$説明": "この一覧の語だけを使う。iOS と Android で行が対応するようにするための固定語彙。該当が無い工程は行を書かなくてよい（無いことも情報なので .md 側に理由を書く）",
    "値": [
      "前提",
      "取得",
      "依存解決",
      "ビルド",
      "検品",
      "仮想機の確認",
      "実機の確認",
      "配布",
      "記録",
      "報告"
    ]
  },
  "依存": {
    "$説明": "その手順が何に縛られているか。**統合できるかどうかはこれだけで決まる**ので、ここを推測で書かない",
    "値": {
      "none": "どの機体でも・どのプラットフォームでも同じ。OS も資格情報も機材も関係しない",
      "platform": "**そのプラットフォームの仕組みに縛られる**（iOS と Android で違うが、機体を替えても変わらない）。例: development 署名版と TestFlight 版は入れ替えられない（Android は同じ鍵なら上書きできる）",
      "env": "その機体の環境に縛られる（OS・文字コード・ウイルス対策・DNS・パスの区切り・ディスク）",
      "credential": "その機体にしかない資格情報に縛られる（署名鍵・配信の資格・API の鍵）",
      "hardware": "その機体にしかない機材に縛られる（実機・仮想機のランタイム）",
      "project": "案件ごとに違う（applicationId・バンドルID・配布先・フラグ）"
    },
    "$なぜ platform を足したか": "2026-09-06 の iOS 側の反証で申し送りが出た。`none` と `env` の2択だと、**プラットフォーム固有の事実が `none` に落ちて `build/` へ統合されてしまう**。統合すると『iOS でも Android でも同じ』という嘘の規約になる。機体（env）とプラットフォーム（platform）は別の軸なので、列を分けずに値を足した"
  },
  "検証状態": {
    "$説明": "書いた内容をどう確かめたか。**確かめていないものを measured と書かない**",
    "値": {
      "measured": "この機体で実際に走らせて確かめた（所要・出力・終了コード）",
      "read": "コードか文書を読んで確かめた（走らせてはいない）",
      "未検証": "記憶で書いた。突き合わせのときは弱い根拠として扱う"
    }
  },
  "行の形": {
    "id": "整数。ファイル内で一意",
    "工程": "上の `工程` の語のいずれか",
    "手順": "何をするか。1行で。**「たぶん」を書かない**",
    "実体": "コマンド・ファイル・行番号。無ければ空文字（無いこと自体が発見）",
    "依存": "上の `依存` のキーのいずれか",
    "依存の理由": "依存が none 以外のとき必須。何に縛られているかを具体で（「Windows だから」は理由ではない）",
    "案件差": "aub | flash | planttalk | 両方 | 全部 | 差あり | 該当なし。`差あり` のときは `案件差の中身` を書く",
    "案件差の中身": "任意。どう違うか",
    "所要": "秒。実測していなければ null",
    "罠": "任意。踏んだ失敗と、その見分け方",
    "出典": "どこを見たか（ファイル:行・コミット・ログ）",
    "検証状態": "上の `検証状態` のキーのいずれか"
  },
  "必須の列": [
    "id",
    "工程",
    "手順",
    "実体",
    "依存",
    "案件差",
    "所要",
    "出典",
    "検証状態"
  ],
  "捕まえないもの": [
    "手順が正しいか。ここは「いま何をしているか」を書き出すだけで、良し悪しは判定しない",
    "統合の可否。両機体が揃ってから、上の `統合の判定規則` を当てて決める",
    "確かめた方法: python3 build/inventory_check.py --self-test"
  ],
  "案件差": {
    "$説明": "その手順がどの案件に効くか。**iOS には PlantTalk（~/planttalk）があり、Android 側には無い**（2026-09-06 実測・design/ を持たないのでハーネス未導入）",
    "値": {
      "aub": "aub-familywalk だけ",
      "flash": "flash-compose（FlashEnglish）だけ",
      "planttalk": "PlantTalk だけ（iOS 側にしか無い案件）",
      "両方": "aub と flash の両方",
      "全部": "PlantTalk を含む全案件",
      "差あり": "案件で値や手順が違う。`案件差の中身` に両方の値を書く",
      "該当なし": "案件に紐づかない（機体の設定など）"
    }
  },
  "粒度": {
    "$説明": "**1手順1行。** 対称な判断（どちらのプラットフォームでも同じ）と、その機体固有のコマンドを1行に混ぜない",
    "なぜ": "混ざった行は依存を1つに決められない。2026-09-06 の iOS 側で「pubspec が動いたら pod install」が実例（判断は none・コマンドは env）",
    "混ざっていたら": "行を2つに分ける。分けずに残すなら `罠` に「対称性あり・統合時に分割が要る」と書く"
  }
}