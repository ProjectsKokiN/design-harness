# 仕組み改善の計画

**2026-09-04 に、開いている課題を通しで読んで作りました。**
58件を読み、失効1件・重複3件を落として **54件**。それを9段にまとめています。

**進捗: 段0〜段3 が完了（20件）。**

| | 件数 |
|---|---|
| 計画に載っていて、まだ開いている | 34 |
| 案件から上がって段へ入れた | 3（#60 → 段8・最優先 / #61 → 段8 / #62 → 段5） |
| 段3 の測定で見つけて起票した | 1（#63 → 段4） |
| **開いている合計** | **38** |

**次にやるのは #60（固定先が古い）です**（2026-09-04 ユーザー確定）。段8 の中の1件ですが、
**ここが全部の直しの通り道**なので、段4 に入る前に片付けます。
段2 では 段3 の #11（条件2 の札）も一緒に閉じています（同じ文書を触るため）。

順番は **「何が直ると次に進めるか」** で決めています。
段0〜3（20件）は**直すための土台**、段4以降（34件）は**中身**です。
土台を飛ばして中身から直すと、直したことを確かめられません。

## 進め方

- 1段を終えてから次へ進む。**段の途中で先の段に手を出さない**
- 直したら Issue を閉じる。**中身を転記してから閉じる**（統合の場合）
- 前提が変わっていたら、閉じずに**題と本文を直す**（#14 がその例）

---

## 段0 — 作業が止まっているのを止める（5件）— **完了 2026-09-04**

**5件とも `tools/machine_scope.py` の中。1回の作業で終わる。**

| 課題 | 何が起きているか |
|---|---|
| **#46** | set -e 下での安全な呼び方が書かれていない。**Mac mini と Windows が2日間、検査を一度も走らせていなかった**（両機とも `--no-verify` で push） |
| **#49** | `--check` が `@{u}...HEAD` の三点比較を使うので、マージ・rebase で取り込んだ他機体のコミットを自分の変更と数える。**push が止まった5回** |
| #29 | `owner_of` は最長一致、`owns` は緩い前方一致。**同じパスに違う答えを返す** |
| #40 | `owns()` / `owner_of()` が `shared` を見ないので、shared に結んだ段が全機体で飛ぶ |
| #7 | `--owns` に `--config` が必須なのに `--help` でそう読めない。手で呼ぶと2回失敗 |

**#46 がいちばん重い。**検査が落ちるのではなく、**静かに1本も走らない**。

## 段1 — 直したことを3台で確かめられるようにする（5件）— **完了 2026-09-04**

**Windows が緑になるまで。**いまは常時 NG なので本物の NG が埋もれる。

| 課題 | 実害 |
|---|---|
| **#6** | 検査が macOS でしか回っていない。3日で5件、**うち2件は検査自身のバグ** |
| #52 | 絵文字を print して cp932 で死に、嘘の理由で落ちる（3段で再現） |
| #41 | 生成物の書き戻しが LF。CRLF の作業ツリーで毎回誤検知 |
| #37 | Figma のトークンを持たない機体で必ず NG。その機体に打つ手が無い |
| #36 | カタログ鮮度の板挟み。どちらを選んでも NG が残る |

## 段2 — 検査が嘘をつくのを止める（5件）— **完了 2026-09-04**

**ハーネスが存在する理由そのもの。**

| 課題 | 実害 |
|---|---|
| **#17** | 空振り5形（例外を捨てる・空の状態しか描かない・期待値の自己参照・緩い finder・直書き文字）。**5件が全部緑。うち3件は実機とユーザーが発見** |
| #12 | 飛ばして緑。条件5と条件7を誰も測っていなかった |
| #30 | `gap_report` が壊れた設定で報告を出す。CI で **119件の嘘の発火** |
| #43 | `extends` がリポジトリ外を指し、CI でだけ走査0ファイル |
| #4 | 元ファイルにある段が案件から落ちても気づけない。3日間、段が一度も走っていなかった |

## 段3 — 書き出しを1つの形にする（5件）— **完了 2026-09-04**

| 課題 | |
|---|---|
| **#14** | 画面のノード木の書き出し器が aub にしかない。**#59・#25・#11 がこれに依存** |
| #21 | 書き出しが「読まなかったもの」を宣言しない。誤読7件のうち3件の根 |
| #25 | 鮮度検査が画面（`frames.json`）を見ていない |
| #11 | 条件2の札を外す（`coverage_check` / `impl_coverage_check` に残存） |
| #59 | 同じ Figma を2回読んでいる。1画面で4〜6回 |

## 段4 — 書き出しの穴を埋める（7件）

| 課題 | 実害 |
|---|---|
| **#8** | `sz=FILL` が実装に届かない。**19件**（実機14＋レビュー5） |
| #48 | `instance_overrides` が画面直下しか降りず、ダイアログの中が1件も出ない |
| #26 | インスタンスの寸法上書きが出ない。写真が全部 48 で潰れた |
| #10 | キャンバススタッキングが無く、AI が「Figma が壊れている」と誤報 |
| #22 | `childSizes` が1変異のサンプルしか持たない |
| #32 | `figma.mixed` で書き出しごと止まる。12部品のうち1件も取れず |
| #16 | 宣言の外に残る 390 の数値。Pro Max で50px ずれ |
| **#63** | 実装のコードに残る手測りの値。**#16 と同じ道具で見る**（2026-09-04 追加） |

**#16 と #63 は1つの道具にします。** どちらも「実装の中だけの数値が誰にも見られていない」形で、
#16 の `82.4`・`254` と #63 の `138.0` は同じ性質です。記録層を消しても、
**手で測った値は消えたのではなく実装の定数へ移っただけ**でした。

## 段5 — 実機と動きを見る（7件 ＋ #62 で8件）

**やろうとしていること: 実機で初めて見つかる壊れを、机の上で見つける。**

いま、机の上の検査は**数値しか見ていません。** 書体の幅・押した瞬間の見た目・画像の切り取りは
数値に出ないので、全部すり抜けます。**これまでに出た壊れは、機械が1件も見つけていません。
全部、人か実機が見つけました。**

| 何が起きたか | 見つけた人 |
|---|---|
| 行が12pxはみ出す・日付に写真が重なる・お題名が切れる（aub） | Mac mini（実機） |
| タイトルのアイコンがダイナミックアイランドに7割隠れる | Mac mini（実機） |
| 下部ナビの文字が「ディクテーショ」で切れる | Windows（実機） |
| 押すと画面が消える（**試験816件は緑**） | ユーザー |
| アプリアイコン20枚のうち14枚が壊れる | ユーザー |

### 4つのまとまりで直します

| まとまり | 課題 | 何をするか |
|---|---|---|
| **1. 押している間を作る** | #27・#28 | いまの試験は指を「触れて離す」だけで、押している間を一度も描かない。指を置いたまま1コマ進める。**Figma に無い状態へ動きうるか**も同時に見る |
| **2. 文字を正しく測る** | #54・#55 | 字間が落ちて8文字で2px短く出る。外へはみ出して描く文字は、幅ではなく「切られる枠に収まっているか」で見る |
| **3. 実装側の重複を数える** | #53 | 部品を直しても手組みの画面に届かない。**いまの分母はどれも Figma 側**で、実装側から見た重複を数える道具が無い |
| **4. 人が目で見る場を作る** | #18・#58・#62 | 大小2台を立ち上げる／状態を指定して画面を開く／アプリアイコンを検査に載せる |

**#27 と #28 はリリース版が落ちた1件の表裏**です。#28 が「なぜ落ちたか」、#27 が
「なぜ試験が見なかったか」。1つの直しで両方閉じます。

**#62（アプリアイコン）をここに置く理由**: 実機でしか見えない成果物で、
「全面に広がる絵を Android の適応アイコンにどう載せるか」は**機械では決まりません**
（A案/B案が並ぶ）。まとまり4の「人が見る」層がその判断の置き場です。

### 順番

まとまり **1 → 2 → 3 → 4**。前2つは実害が最も重く、後半は道具より段取りだからです。

### この段が変えること

- 押して落ちるものは、送る前に止まります（いまは実機に配ってから分かります）
- 文字の切れが机の上で分かります（いまは実機の報告待ちです）
- 実機を見るのが「思い出したらやる」から「毎回やる」に変わります

**見た目が良いかどうかの判断は、これまでどおりデザイナー本人に残ります。**
この段が増やすのは「壊れているかどうか」を機械で言える範囲だけです。

## 段6 — 設計の穴（7件）

#57（画面をまたぐ矛盾）／ #24（書き出しの中の矛盾を検算していない）／ #20（同じ状態を2か所に持つ）／
#9（安全領域の共通の決まり）／ #19（決定の取り消しが書けない）／ #45（複数担当にまたがる1つの値）／
#23（部品が Figma の数値を持つ検査の穴）

## 段7 — 道具の契約（6件）

#2（binary が読めない・#3 統合済み）／ #5（生成物を上書きして戻さない）／ #33（`gen_*` の規則が書かれていない）／
#34（対応表の形を1つしか受けない）／ #35（`pages` の散文）／ #39（発火ログの項目表が無い）

## 段8 — CI と運用（7件 ＋ #60・#61 で9件）

**やろうとしていること: 直したものが、他のパソコンに届くようにする。**

いま4案件とも、仕組みの固定先が **9〜25コミット遅れ**ています（2026-09-04 実測）。
**段0から段3の直しは、どの案件にも1つも入っていません。**

もっと悪いのは、届いていないことが**自分のパソコンでは分からない**ことです。5通りあります。

| 何が届いていないか | 何が起きたか |
|---|---|
| 仕組みの直し（固定先が古い・**#60**） | すでに直した不具合を16コミット遅れで踏み直した |
| デザインの書き出し（**#51**） | 2日間、1台でしか通らない状態だった |
| 3台への依頼書（**#42**） | 527行の依頼が2日間、相手に届いていなかった |
| 変更の説明（**#56**） | 入っていない変更を「入っている」と書き、2台に無駄なビルドをさせた |
| 自動検査そのもの（**#44**） | 数時間、検査が1度も動いていないのに、動いていると思って直し続けた |

**全部「自分のパソコンだけ緑」です。** 他のパソコンが赤くなって初めて分かります。
**git のブランチという境界と、共有物の境界がずれている**のが共通の根です。

### 3つのまとまりで直します

| まとまり | 課題 | 何をするか |
|---|---|---|
| **1. みんなに見えているかを見る** | #60・#51・#42・#56・#44 | いまの検査は「中身が正しいか」しか見ていない。**「この直しは他の人から見える場所にあるか」**を足す。上の5つはこの1つの見方でほとんど捕まる |
| **2. 送る前の検査の行き止まりを無くす** | #13・#61 | 担当外のファイルを直さないと自分の検査が通らないとき、逃げ道が無い。枝を消すだけでも検査が丸ごと3分走り、掃除が時間切れになる |
| **3. 残り** | #31・#15 | #31 は `/harness-issues` の走査範囲。#15 は `--has-token` を足すだけで、**最も小さい** |

### #60 を最優先にします

**ここが閉じないと、段4から段7で作るものも同じように届きません。** 段8の中ではなく、
**段4に入る前**に片付けます。

調べたところ、#60 の本文より悪い状態でした。

- `pin_check.py` は**すでにあり、段にも載っています**。ただし
  `python3 …/pin_check.py 2>/dev/null || true` と呼ばれており、
  **出力を捨てて必ず成功します**
- この行を持っているのは3案件のうち **aub だけ**（flash-compose と planttalk は0箇所）
- 段2で作った `stage_check --stages` は `verify.sh` と CI を見ますが、
  **`pre-push` を見ていません**。だから「呼んでいない案件がある」も見えません
- `pin_check.py` は **`--self-test` を持ちません**（本文に「持たない」と明記されている）

### 順番

**#60 → 共有の4件（#51 #42 #56 #44）→ 送る前の2件（#13 #61）→ #31 → #15**

### この段が変えること

- 他のパソコンが赤くなって初めて分かる、が無くなります
- 無駄なビルドの依頼が減ります（1回あたり2台ぶんの時間です）
- 枝の掃除が3分から数秒になります
- **段4から段7で作ったものが、実際に案件へ届きます**

---

## 通しで読んで分かったこと（2026-09-04）

### 落としたもの

| | 判定 | 根拠 |
|---|---|---|
| #38 | **失効** | `harness_stats.py` はすでに `design_check.load_rules()` を再利用しており、extends を写経していない |
| #3 | #2 に統合 | 同じ生成器の同じ1行。#3 自身が「2つとも直さないと1行に戻せません」と書いていた |
| #50 | #49 に統合 | 原因が同じ（`@{u}...HEAD` の三点比較）。実害は合計5回 |
| #47 | #8 に統合 | 根が同じ（`sz=FILL` を写す規則が無い）。実害は合計19件 |

### 前提が変わっていたもの

**#14** は「書き出し器がどこにも無い」で起票されていたが、**aub には `export_frames.js` があり `frames.json` は画面のノード木そのもの**だった（31ノード・実測）。題を「共有層に上がっていない」に直した。

### 重複に見えて、重複でなかったもの

**書いた本人が明示的に区別していたものは残した。**

- #12 と #17 — 「段が飛ぶ」と「段は回っているのに中身が空」
- #8 と #16 — #16 は「#8 の突き合わせでは捕まらない」と明記
- #10・#22 と #21 — #21 は根、#10・#22 は個別の欠落。両方直す必要がある
- #36・#37・#41 — 症状は同じ（Windows で常に NG）だが原因が3つ別
- #54 と #55 — 同じ症状から出た別原因


---

## 済んだ段の記録

### 段0（2026-09-04・`7156a2b` `7f2e95b`）

5件とも `tools/machine_scope.py`。**担当判定を1か所にした**のが本体で、
`owns()` は `owner_of()` の答えをそのまま返すようにした。

ついでに起票されていない潜在バグを2件見つけて直した。

- `norm()` の `lstrip("./")` が**文字を剥がす**ので `.harness_log.jsonl` が
  `harness_log.jsonl` になっていた
- 素の `startswith` なので `design/` の宣言が `designs/x` にも当たっていた

`ap.error()` が `SystemExit` を投げるため呼び出し側が終了コードで分岐できない
ことにも気づき、他の設定エラーと揃えて `return 2` にした。

### 段1（2026-09-04・`b160f24` `125f289` `926a9b2`）

| 課題 | 何をしたか |
|---|---|
| #52 | `tools/_utf8.py` を新設し全31本に入れた。cp932 で**死ぬ道具 2本 → 0本** |
| #6 | `tools/portable_check.py` を新設。**実在の Windows 非互換11件**を見つけて直した |
| #41 | 改行だけの差を「担当外を変えた」と数えないようにした |
| #37 | **誰も担当していない段は落とす**ようにした（黙って飛ばさない） |
| #36 | aub で既に直っていた。**「宣言しない。導出する」**を `DESIGN.md` に上げた |

`_utf8` の import 3行で `gen_io` と `figma_freshness` が網羅 50% を割ったので、
**閾値を動かさず self-test を厚くした**（最小 49% → 52%）。

`gen_io` に足したのは `load()` の件数照合が**書き出しの種類ごとに本当に落ちるか**
（9種類）。測定の都合で足したが、**実質的な穴だった**——通っていない分岐の
書き出しは、件数が合わなくても黙って通る。


### 段2（2026-09-04・`7bee93e` `d71bad4` `356db83`）

**分母を3つ作った段。** どれも「無いことに気づけない」を潰している。

| 課題 | 何をしたか |
|---|---|
| #30 | ラチェットの判定を `engine` の `ratchet()` に1本化。**壊れた設定のまま報告を出さない** |
| #43 | `ci_path_check --rules`。extends がリポジトリ外を指したら落とす |
| #12 | `gen_gate.py` で関門の条件を**正本から生成**。`skip_gate` と条件の掃き出し |
| #4 | `stage_check --stages`。元ファイルの段が落ちたら落とす。宣言は `design/stages.json` |
| #17 | `hollow_check.py`。空振りの4形＋仕込み試験の記録 |
| #11 | 条件2 の札を「参考」に付け替え（段3 から前倒し） |

**分母の鎖はこうなった。**

    production-gate.md（正本）→ gate/conditions.json（生成物）
      → 元ファイルの段の札 → 案件が走らせている段

3つとも**導出している。手で書いていない。**

#### 仕込み試験で分かったこと（3件）

1. **`skip_gate` の `FAILED=1` は、最初の7件では一度も試されていなかった。**
   条件の掃き出しが先に捕まえていたため。番号を持たない段を飛ばす例を足して
   isolate した
2. **網羅の計測が入れ子で外側の追跡係を壊していた。** `stage_check` 自身が
   53% と表示していたが、実際には 27% までしか測れていない。**計測そのものが
   嘘をついていた形**で、この段が捕まえるはずのものだった。直したら
   中央値が 67% → 69%、最大が 74% → 77% に上がった（他の道具も過小だった）
3. **`readme_check` の正規表現が数字を含む名前を拾えず**、`_utf8.py` は
   載せても永久に「未掲載」と出ていた

#### `hollow_check` の実測（aub-familywalk）

初回 15件のうち **9件が誤検出**だった。分母から外したもの:

- 検査ファイルが**自分で宣言した名前**（期待値を組み直すのは正しい書き方）
- **生成物 `.g.dart` を呼ぶ期待値**（それは書き出しで、この仕組みが求めている形）

残った6件は全部本物。ほかに**誰も見ていない文字が6件**（アプリ名を含む）。


### 段3（2026-09-04・`b5e1b8f` `8f60eb2` `624eb5f`）

**画面と、書き出しが読まなかったものを見えるようにした段。**

| 課題 | 何をしたか |
|---|---|
| #25 | 鮮度に**画面**の面を足した。画面1枚ごとの指紋。部品と切り分ける |
| #14 | `export_frames.js` を aub から回収。`screen_export_check.py` で前提条件を測る |
| #21 | `gen_notcaptured.py` で「読めていないプロパティ」を生成。`gen_io.absent()` で問える |
| #59 | 書き出し優先に手順を変えた。境界は `notcaptured.json`（機械可読） |

#### 前提が違っていたもの（1件）

**#14 は「書き出し器が無い」ではなく「aub にしか無い」でした。** aub の
`export_frames.js` は 128 行の成熟した器で、31画面の木を出しています。
無いのは 414 と flash-compose です。**作るのではなく上げるのが正しい直し方**でした。
起票時の前提を実測で確かめてから着手したので気づけています。

#### 実測

| 測ったもの | 結果 |
|---|---|
| aub の器が読めていないプロパティ | 一覧36件のうち **14件**（`strokeIndividualWeights` を含む——「下線だけのタブを四角い箱で囲んだ」失敗そのもの） |
| aub の画面の書き出し | 索引31枚を**すべて木で**持つ。記録層0 |
| flash-compose | `frames.json` が**1つも無い**。`design/values/` が6ファイル |
| aub の画面の実装が使う値 | 数値 132個中 **書き出しに無いのは4個**（97.0%）／トークン 65個中 **0個** |
| 読む量 | 書き出しの1画面ぶんは中央 **1.5KB**。MCP は同じ画面の XML とコード |

**一覧そのものにも穴がありました。** 誤読1件目の原因 `itemReverseZIndex` が
`figma-properties.json` に載っていませんでした。分母に穴があると
「読めていない0件」が嘘になります。

#### 途中で見つけた別の穴（2件）

1. `run_main`（鮮度の self-test の実行係）が内側で標準出力を飲んでいて、
   **報告の文言を一度も試験できていなかった**。名指しするのが本題の段なので、
   文言まで見る実行係を作った
2. `portable_check` が**素の `run()` を `subprocess.run` と取り違えて誤検出**した。
   この道具自身が嘘をついた形

#### 残した仕事

- **#59 の後半は未確認。** 「書き出しの中身が足りている」は数字で示したが、
  「MCP を止めても理解が落ちない」は次に画面を1枚実装するまで確かめられない
- **#63 を起票**（実装のコードに手測りの値が残っていても、どの検査にも
  引っかからない）。aub に1件残っていた——記録層は消えたのではなく、
  **実装の定数へ移っただけ**だった
